version: '3.8'
services:
  postgres:
    image: postgres:15-alpine
    container_name: subify_postgres
    environment:
      POSTGRES_USER: subify_admin
      POSTGRES_PASSWORD: SecretPassword123!
      POSTGRES_DB: subify_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  postgres_data: